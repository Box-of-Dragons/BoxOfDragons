name: Deploy to Production

# Manual deploy-only fallback. Normal deploys happen as part of the Release
# workflow — see StructuredChaos docs/git-rules.md.

on:
  workflow_dispatch:

jobs:
  deploy:
    uses: Box-of-Dragons/StructuredChaos/.github/workflows/family-deploy.yml@master
    with:
      branch: master
      environment: production
    secrets: inherit
